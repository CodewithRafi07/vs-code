#include <stdio.h>

int main(){
    // Explain step to step evaluation of 3*x/y - z+k, where x=2, y=3, z=3, k=1
    int x=2, y=3, z=3, k=1;
    float e = 3*x/y - z+k;
    printf("The value of e is %f" , e);
    return 0;
}